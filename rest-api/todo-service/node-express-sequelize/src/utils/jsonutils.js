/**
 * Basic function used to setup Jest
 * @param a first number
 * @param b second number
 * @returns {*} the sum of the given two numbers
 */
function sum(a, b) {
    return a + b;
}

module.exports = sum;
